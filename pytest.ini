[pytest]

addopts = --strict
asyncio_mode = auto
filterwarnings =
    error
    ignore:create_react_agent has been moved to `langchain\.agents`.*:langgraph.warnings.LangGraphDeprecatedSinceV10
markers =
    allow_network: test legitimately needs a real network connection (the unit
        suite otherwise fails any test that opens one)
