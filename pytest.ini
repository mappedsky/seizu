[pytest]

addopts = -p no:warnings --strict
norecursedirs = .git
asyncio_mode = auto
markers =
    allow_network: test legitimately needs a real network connection (the unit
        suite otherwise fails any test that opens one)
